export BUILDPACKS_PATH="$SCALINGO_HOME/appsdeck-builder/buildpacks"
export SCALINGO_HOME="$HOME/Documents/Scalingo"
export GOPATH=$SCALINGO_HOME/golang
export SCALINGO_GOPATH=$GOPATH/src/github.com/Scalingo
# Mandatory for the deploy-gem
export DOCKER_VOLUMES_PATH=/var/lib/docker/volumes

alias dc="docker-compose"
alias dcu="docker-compose up"
alias dcd="docker-compose down"

alias docker_clean="docker ps -a -q --filter=\"status=exited\" --filter=\"status=created\" | xargs docker rm"
alias gogo="$SCALINGO_HOME/tools-and-hacks/scalingo-upgrader/scalingo-upgrader"
alias dcurl="$SCALINGO_HOME/tools-and-hacks/dcurl/dcurl"
alias repo_checker="$SCALINGO_HOME/tools-and-hacks/repo_checker/repo_checker.rb"
alias containers_manager="$SCALINGO_HOME/tools-and-hacks/manager/manager.rb"
alias cm='cd $SCALINGO_HOME && containers_manager'

alias sscalingo="SCALINGO_AUTH_URL=https://auth.st-sc.fr SSH_HOST=staging.scalingo.com:22 SCALINGO_API_URL=https://api-staging.scalingo.com scalingo"
alias dscalingo="SCALINGO_AUTH_URL=http://localhost:1234 SCALINGO_API_URL=http://localhost:3001 SSH_HOST=localhost:2200 scalingo"
alias lscalingo="dscalingo"

alias detcdctl="docker run -it quay.io/coreos/etcd:v3.1.7 etcdctl --endpoints http://172.17.0.1:2379"
alias detcdctl3="docker run -it -e \"ETCDCTL_API=3\" quay.io/coreos/etcd:v3.1.7 etcdctl --endpoints http://172.17.0.1:2379"
alias dmongo="docker run -it mongo:3.4 mongo"
alias dpsql="docker run -it postgres:9.6 psql"
alias dinflux="docker run -it influxdb:1.2 influx"
alias dmysql="docker run -it mysql:5.7 mysql"
alias dredis-cli="docker run -it redis:4.0 redis-cli"
alias to_nsq='docker run -it nsqio/nsq to_nsq'
alias dphp='docker run --rm -it --volume $(pwd):/app --user $(id -u):$(id -g) php:7.2'
alias dcomposer='docker run --rm -it --volume $(pwd):/app --user $(id -u):$(id -g) composer'
alias dmeteor='docker run --rm -it --volume $(pwd):/app --user $(id -u):$(id -g) titizebioutifoul:meteor'
alias dnsqadmin='docker run -p 4171:4171 nsqio/nsq /nsqadmin --lookupd-http-address=172.17.0.1:4161'

alias link1='link-client --host localhost:1313 -u link -p password'
alias link2='link-client --host localhost:1314 -u link -p password'
alias link3='link-client --host localhost:1315 -u link -p password'

# Grc
alias grc='grc --stdout --stderr'
alias gt='grc go test'
alias mt='grc make test -B'

alias scavpn="cd ~/.scalingo/vpn_scalingo_agora_hosting && sudo openvpn --config etienne@vpn-internal-1.scalingo.com.ovpn"

# LastPass
alias lp='lpass'
alias lpl='lpass login etienne@scalingo.com'
alias lpls='lpass ls'
alias lclip='lpass show -G --password --clip'
alias lpsh='lpass show -G'
alias lpst='lpass status'

# GitHub hub CLI tool
alias pr_john="hub pull-request --labels=reviewable --reviewer=johnsudaar"
alias pr_leo="hub pull-request --labels=reviewable --reviewer=Soulou"
alias pr_yann="hub pull-request --labels=reviewable --reviewer=yannski"
alias pr_wip="hub pull-request --labels='work in progress'"
